import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function AdminSchedulePage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Global Schedule Management</CardTitle>
                <CardDescription>View and manage schedules for all specialists. This feature is under construction.</CardDescription>
            </CardHeader>
        </Card>
    );
}
